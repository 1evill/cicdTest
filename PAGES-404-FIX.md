# GitHub Pages 404 错误修复指南

## 🚨 问题描述

访问 `https://1evill.github.io/cicdTest/` 时出现 404 错误。

## 🔍 可能的原因

### 1. **GitHub Pages 未启用**
- 仓库没有启用 GitHub Pages 功能
- 分支选择不正确

### 2. **部署失败**
- CI/CD 构建失败
- 没有成功推送到 `gh-pages` 分支

### 3. **配置问题**
- Vite 构建路径配置错误
- `base` 路径设置问题

### 4. **DNS 生效时间**
- GitHub Pages 需要时间生效（通常 1-10 分钟）

## ✅ 解决方案

### 方案1：检查并配置 GitHub Pages（必须执行）

#### 步骤1：进入仓库设置
1. 打开 GitHub 仓库：`https://github.com/1evill/cicdTest`
2. 点击 `Settings` 标签

#### 步骤2：配置 Pages
1. 左侧菜单选择 `Pages`
2. 在 `Build and deployment` 部分：
   - **Source**: 选择 `Deploy from a branch`
   - **Branch**: 选择 `gh-pages`
   - **Folder**: 选择 `/ (root)`
3. 点击 `Save`

#### 步骤3：等待部署
- 保存后会显示 "Your site is ready to be published at..."
- 等待 1-10 分钟让配置生效

### 方案2：修复 Vite 配置（已完成）

已修复 `vite.config.ts`：
```typescript
export default defineConfig({
  plugins: [vue()],
  // 设置 GitHub Pages 基础路径
  base: "/cicdTest/",  // ✅ 新增：修复路径问题
  build: {
    sourcemap: false,
    minify: "terser",
  },
});
```

### 方案3：重新构建和部署

#### 步骤1：推送修复后的配置
```bash
git add .
git commit -m "修复 GitHub Pages 路径配置"
git push origin main
```

#### 步骤2：检查 CI/CD 运行
1. 进入 GitHub 仓库的 `Actions` 页面
2. 确认最新工作流程运行成功
3. 检查是否有错误信息

#### 步骤3：验证 gh-pages 分支
1. 进入仓库的 `Branches` 页面
2. 确认 `gh-pages` 分支存在且有内容
3. 检查分支中有 `index.html` 文件

## 🔍 故障排除

### 检查 1：GitHub Pages 状态
在仓库的 `Settings` → `Pages` 页面查看：
- ✅ 状态显示 "Your site is published at..."
- ❌ 如果显示 "GitHub Pages is disabled"，需要重新启用

### 检查 2：CI/CD 运行状态
在 `Actions` 页面查看：
- ✅ 工作流程显示绿色对勾
- ❌ 如果失败，查看错误日志并修复

### 检查 3：分支内容
检查 `gh-pages` 分支是否包含：
- `index.html`（主页面）
- `assets/` 文件夹（CSS、JS 文件）

## 🛠️ 高级修复选项

### 选项1：使用根目录部署
如果不想要 `/cicdTest/` 路径，可以：

1. 修改仓库名为 `1evill.github.io`
2. 或使用自定义域名
3. 或修改 `vite.config.ts` 中的 `base` 配置

### 选项2：检查构建输出
本地测试构建：
```bash
npm run build
ls -la dist/  # 检查构建产物
```

### 选项3：手动部署
如果自动部署失败，可以手动：
```bash
# 构建项目
npm run build

# 推送到 gh-pages 分支
git add dist/ && git commit -m "Deploy"
git subtree push --prefix dist origin gh-pages
```

## 📊 验证清单

- [ ] GitHub Pages 已启用
- [ ] 选择 `gh-pages` 分支作为源
- [ ] Vite 配置中 `base: "/cicdTest/"`
- [ ] CI/CD 工作流程运行成功
- [ ] `gh-pages` 分支存在且有内容
- [ ] 等待 DNS 生效（1-10 分钟）

## 🆘 获取帮助

### GitHub 官方文档
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [故障排除指南](https://docs.github.com/en/pages/getting-started-with-github-pages/troubleshooting-jekyll-build-errors-for-github-pages-sites)

### 常见 404 原因
1. **Repository name 不匹配** - 确保 URL 正确
2. **分支选择错误** - 必须选择 `gh-pages`
3. **构建失败** - 检查 CI/CD 日志
4. **路径配置错误** - Vite `base` 配置问题
5. **文件不存在** - 检查 `gh-pages` 分支内容

## 📞 进一步帮助

如果问题仍然存在：
1. 提供 GitHub 仓库链接
2. 分享 CI/CD 错误日志
3. 截图 GitHub Pages 设置页面

完成以上步骤后，你的网站应该能在 `https://1evill.github.io/cicdTest/` 正常访问！